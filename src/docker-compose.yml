version: '3'
services:
  web:
    build: .
    ports:
      - "8888:5000"
    volumes:
      - "/tmp/videos:/usr/storage"
  db:
    image: mongo
    restart: always
    volumes:
      - "/tmp/mongodb:/data"
    # environment:
    #   MONGO_INITDB_ROOT_USERNAME: mongodb
      # MONGO_INITDB_ROOT_PASSWORD: 
